apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: production

configMapGenerator:
- name: configmap
  namespace: production
  behavior: replace
  envs:
  - config.properties

resources:
- ../../bases

patches:
- path: replicas.yaml
