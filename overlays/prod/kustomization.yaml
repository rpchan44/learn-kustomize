apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: trunk

configMapGenerator:
  - name: configmap  
    envs:
      - config.properties 

resources:
- ../../bases

patches:
- path: replicas.yaml
